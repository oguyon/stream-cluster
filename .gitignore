# Benchmarks
benchmarks/*.txt
benchmarks/*.mp4
benchmarks/*.png
benchmarks/benchmark_out/
benchmarks/clusteroutdir/
benchmarks/*.clusterdat/
benchmarks/benchmark_summary.md
!benchmarks/test_strat.txt 
# (Keep test_strat.txt if it was original, but looking at file list it seems generated or moved. 
# Actually, I'll just ignore generated ones. The original benchmarks.bash logic generates them.
# Except if some are source data. 2Dspiral.txt etc are generated by mktestseq.)

# test
test/

# Build
build/
bin/
lib/
*.o
*.a
*.so

# Output
*.clustered.txt
discarded_frames.txt
out_discard/
out_merge/
data/
*.log

# Temp
test_birch_stream.py
test_strat.clustered.txt
